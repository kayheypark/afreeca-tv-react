import styled from 'styled-components';
import FlexY from 'component/atom/FlexY';

const ListSection = styled(FlexY).attrs({ as: 'section' })`
    padding-top: 32px;
    padding-left: 46px;
    padding-right: 46px;
`;

export default ListSection;
